<Window x:Class="TheDebtBook.Views.AddDeptToDebitorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TheDebtBook.Views"
        mc:Ignorable="d"
        Title="AddDeptToDebitorView" Height="450" Width="800" DataContext="{Binding Source={StaticResource ViewModelLocator},Path=DebtViewModel}">
    <Window.Resources>
        <Style x:Key="TextAlignmentRight" TargetType="{x:Type TextBlock}">
            <Setter Property="TextAlignment" Value="Right" />
        </Style>
    </Window.Resources>
    <Grid>
        <DataGrid ItemsSource="{Binding DebtsDatesValues}" Margin="10,76,383,132" IsSynchronizedWithCurrentItem="True" FontSize="20" AutoGenerateColumns="False" SelectedIndex="{Binding Path=CurrentIndex}">
            <DataGrid.Columns>
                <DataGridTextColumn MinWidth="100" Width="3*"  Header="Date" Binding="{Binding Date}"></DataGridTextColumn>
                <DataGridTextColumn MinWidth="60" Width="2*" Header="Dept" Binding="{Binding Value}" ElementStyle="{StaticResource TextAlignmentRight}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
